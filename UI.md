目前整个设置页的痛点非常典型：**控件样式不统一（原生的 Win32 勾选框与不规则的 ComboBox 混杂）、输入框厚重沉闷、各功能页留白不一致、列表数据（如高级诊断）呈现像草稿。**

为了将 **`Hermes`** 的全局控制中心打造成完美的“果系极简面板”**，我将为你梳理出一份**完整的、可以直接作为 Agent 最终执行标准的设置主窗口 UI/UX 视觉重构方案。整个窗口将完全剥离原生 Windows 边框，转而使用自研的云母（Mica）材质卡片体系。

---

# 🌐 Hermes 设置主窗口 UI/UX 全页视觉重构方案

## 1. 窗口基础骨架与全局主题 (Global Shell)

* 
**物理规格**：固定尺寸 `800px × 600px` ，`ResizeMode="NoResize"`。


* 
**无边框窗口 (WPF Shell)**：设置 `WindowStyle="None" AllowsTransparency="True" Background="Transparent"` 。


* **背景材质 (Mica Texture)**：
* 
**深色模式**：主体背景色为深邃墨黑 `#0A0A0C`（比 `#121212` 更高级），背后注入 Windows 11 原生云母（Mica）模糊，使其带有轻微的桌面壁纸透光感 。


* **整体微光描边**：卡片最外层 Border 给予 `1px` 的微光渐变描边。上方为 `#2C2C30`，平滑渐变到下方的 `#1A1A1E`。圆角一律统一为 `CornerRadius="14"`。


* **自定义导航头 (Header)**：
* 窗口最顶部 `32px` 高度空间作为自定义无痕标题栏，支持鼠标拖动移动窗口（`DragMove`）。右侧放置三个极简 SVG 按钮（最小化、关闭 `[×]`） 。





---

## 2. 三段式网格排版系统 (Window Grid Architecture)

页面整体由一个三行布局的 `Grid` 承载：

```
+-----------------------------------------------------------------------+
|  ( ⚙️ )  Hermes                                         [Ctrl+Alt+E]  | <- Header (90px)
|  [常规]   [翻译]   [快捷键]   [外观]   [隐私]   [高级]                |
|───────────────────────────────────────────────────────────────────────|
|                                                                       |
|   核心内容展示区（ScrollViewer 包装，滚动条采用极简无痕 4px 细条）    | <- Body (446px)
|                                                                       |
|───────────────────────────────────────────────────────────────────────|
|  [⚙️ 测试连接]  [🗑️ 清空历史]                               [确定保存] | <- Footer (64px)
+-----------------------------------------------------------------------+

```

### 2.1 品牌与苹果风文字导航头（Header，高度 90px）

* **品牌栏（左侧对齐）**：
* **Logo**：收紧至 `36px × 36px`，带 `CornerRadius="8"` 的微圆角框，在其后方垫一层不透明度仅为 `10%` 的蓝紫渐变光晕。
* **主标题**：`FontSize="18px"`，`FontWeight="Bold"`，前景色 `#FFFFFF`。
* **副标题文案**（清除原有散乱文字）。


* **全局快捷键胶囊（右侧对齐）**：
* 摒弃图中的深蓝色气泡，将其改造成一个精致的**实体拟态微型键帽组（Keycaps Component）**。背景为透明黑 `#000000`(0.2)，边框 `#2C2C2E`。内部的 `Ctrl`、`Alt`、`E` 分别由一个个微型的暗灰色独立小方块隔开，字体采用 `Segoe UI Variable Display`，呈 `#8E8E93` 银灰色，传递极客硬核质感。


* **Tab 导航菜单**：
* 6个页签在一水平线上等距排开，无任何背景色块干扰。
* 
**未激活**：文字颜色 `#8E8E93` 。


* **Hover**：文字平滑变白。
* **激活状态**：文字呈纯白 `#FFFFFF`，同时在页签下方 `6px` 处平滑展开一条厚度为 `2px` 的**科技蓝紫双色渐变微光指示线（LinearGradientBrush：#007AFF -> #5856D6）**。切换页签时，此指示线通过 WPF 动效平滑地“滑向”新页签。



### 2.2 底部全局控制栏（Footer，高度 64px）

* 顶部加一条横向贯穿、厚度仅为 `1px` 的暗色微光边界线（`#2C2C2E`）。
* **左侧并列（调试动作）**：`[⚙️ 测试连接]` 与 `[🗑️ 清空历史]`。背景色为极为克制的灰色 `#FFFFFF`(0.04)，`CornerRadius="16"` 胶囊圆角。
* 
**右侧对齐（保存动作）**：`[确定保存]` 按钮。彻底废除图中的高饱和度刺眼死蓝色。改用具有微光感、深邃的苹果科技蓝（`#007AFF`），前景色为纯白 。Hover 时通过 `Storyboard` 在按钮上方叠入一层极其淡雅的微白高光，科技感拉满。



---

## 3. 六大核心功能面板像素级 UI/UX 方案 (Body Content Sheets)

为了解决画面杂乱、选项平铺的问题，Body 区域内所有配置一律封装进 **“圆角隔离卡片组”（Grouped Capsule Cards）** 中。卡片背景采用 `#FFFFFF` (不透明度 0.03)，边缘采用 `1px` 极淡的 `#2C2C2E` 描边，圆角统一为 `10px`。

---

### Panel 1：【常规】设置页重构 (对应 image_0a7df0.png)

```
+-- Group Card: 基础行为监控 -------------------------------------------+
|                                                                        |
|  ⚡ 启用全局划词翻译                                            [ 🔘 ] |
|  └ 当鼠标在任意窗口拖选或双击英文字符后，自动在选区边界衍生微光触发按钮|
| ────────────────────────────────────────────────────────────────────── |
|  🚀 开机时自动启动 Hermes                                      [ 🔘 ] |
|  └ 允许信使在 Windows 登录时静默常驻系统托盘，后台空闲内存 <= 45MB     |
|                                                                        |
+------------------------------------------------------------------------+

```

* **控件升级（CheckBox 变 ToggleSwitch）**：
* 彻底移除目前图中生硬的 `☑ 开启` 传统复选框。
* 统一使用 **平滑滑动开关（Toggle Switch）**。关闭时轨道为暗灰色 `#2C2C2E`，开启时轨道在 `150ms` 内平滑变为蓝紫渐变色，圆形滑块向右顺滑位移 `16px`。


* **排版细节**：左侧除了主配置文本外，下方必须使用 `FontSize="11px"`、颜色为 `#8E8E93` 的小字打印**功能细则解释**（如上图所示），这能瞬间提升界面的工业级精细度。

---

### Panel 2：【翻译】设置页重构 (对应 image_0a7e26.png)

当前版本的输入框和下拉框高低不一、厚重且边缘突兀。

* **Card 组 A：[ AI 转译核心凭据 ]**
* **Provider（服务商）与 Model（模型）**：将原本黑乎乎的下拉框与输入框样式全面对齐。统一使用高度 `32px`、`CornerRadius="6"`、背景为 `#FFFFFF`(0.04) 的极简胶囊框。下拉菜单（ComboBox）的展开箭头改用 1px 的极简 SVG 下箭头，展开后的 Popup 必须支持亚克力半透明，且带有与主窗体相同的圆角和阴影特效。
* **Base URL & API Key 输入框**：
* 移除输入框内部粗糙的灰色描边，默认使用 `#FFFFFF`(0.04) 弱化背景。
* 
**密文保护机制**：API Key 输入框右侧内置一个 `16px` 的极简“小眼睛”SVG 图标，支持点击平滑切换明文/密文展示。输入框失焦后，下方原有大块粗糙的提示字“已保存...”缩减为一行灰色弱提示：“`凭据已通过 Windows DPAPI 执行硬件级物理加密存储`” 。






* **Card 组 B：[ 翻译个性化偏好 ]**
* 
**目标语言**：直接展示为高亮灰文本：“`简体中文 (Simplified Chinese)`” 。


* 
**翻译风格下拉菜单**：选项统一包装为地道的高级文案：“`自然流利 (Natural)` / `严格直译 (Literal)` / `学术专业 (Professional)` / `极简精炼 (Concise)` ”。





---

### Panel 3：【快捷键】设置页重构 (对应 image_0a7e2f.png)

原图中的快捷键录制框极其突兀。

* **Card 组：[ 兜底触发配置 ]**
* **划词翻译快捷键录制框**：
* 宽度收紧为 `240px`，居右对齐。背景色采用深邃的 `#000000`(0.35)。
* **录制交互动效**：当用户点击该输入框时，光标进入录制状态，输入框的 1px 边框在 `200ms` 内亮起一层科技蓝色的高光呼吸灯特效（`DropShadowEffect` 蓝色，Blur=10）。
* 
**按键捕获状态**：当用户按下组合键时，框内文字实时更替（例如 `Ctrl + Alt + E`） ，且每个按键前景色使用纯白。框下方跟一排小字灰色解释：“`当特定受保护窗口（如密码框、安全终端）无法直接读取选区时，按此键将自动调用受控剪贴板恢复技术进行无痕翻译兜底`” 。







---

### Panel 4：【外观】设置页重构 (对应 image_0a7e4b.png)

原图中由下拉框和普通数字输入框组成，完全体现不出“美学外观”的定位。

* **Card 组：[ 视觉材质与规格 ]**
* **主题（System / Dark / Light）**：废除传统的下拉框。改用苹果标志性的 **“分段选择器”（Segmented Control）**。这是一个横向的长条胶囊框，内部包含“跟随系统”、“深色模式”、“浅色模式”三个并列的平铺文字项。用户点击某一项时，一个白色半透明的圆角滑块垫片（Slider Block）会平滑地滑到对应文字下方，视觉质感极强。
* **卡片宽度 / 译文字号 / 卡片透明度**：
* 摒弃传统的生硬数字输入框，引入 **高级极简微型滑块（Slider）**。
* 进度条轨道为 3px 粗细的暗灰线条，已滑过区域呈现科技蓝色。滑块为一个直径 `12px` 的纯白正圆。滑块右侧实时显示当前数值（如：`420 px` / `14 pt` / `100%`），方便用户拖动时进行实时视觉交互。





---

### Panel 5：【隐私】设置页重构 (对应 image_0a7e68.png)

* **Card 组：[ 数据隐私保护管线 ]**
* **保存翻译历史 / 保存历史时包含原文**：一律转化为平滑的 **ToggleSwitch 开关**。
* 
**安全沙箱隔离区提示**：原本界面上散落的大段提示文本，全部统一收纳在卡片最底部的独立通告栏中，左侧配以一个 `14px` 的微型“🔒 隐私保护”SVG 图标，右侧文本修改为更具安全感、严谨的文案：“`HermesCore 安全审计：应用默认不上传任何未主动触发的文本，本地日志中已被强行脱敏，严禁记录您的 API Key、完整原文及任何敏感资产信息。`” 





---

### Panel 6：【高级】设置页重构 (对应 image_0a7e87.png)

目前版本的“最近触发诊断”列表非常粗糙，生硬的黑色长条色块层层堆叠，且“刷新”按钮样式不统一。

* **右上角 [🔄 刷新列表] 按钮**：
* 改造成胶囊圆角微型按钮，高 `26px`，放置在“最近触发诊断”卡片标题的右侧，两端对齐。背景为柔和的 `#FFFFFF`(0.05)。


* **高级诊断数据列表 (ListView 重构)**：
* 彻底干掉原图中粗糙的暗黑色长条色块和不规范的行间距。
* **列表容器**：使用透明背景。每一条诊断日志作为一行，行高 `36px`。行与行之间不使用大色块隔离，而是使用厚度仅为 `1px`、不透明度为 `2%` 的极其浅淡的微白分割线划开。
* **数据项精细对齐与色彩映射（像素级对齐规范）**：
* **时间戳 (第一列)**：固定宽度 `70px`，前景色采用中性暗灰 `#666666`，字体 `FontFamily="Consolas"` 保证等宽对齐。
* **触发状态 (第二列)**：固定宽度 `110px`。根据数据类型动态映射高亮色标签（不带色块背景，仅文字着色）：若为 `button-shown`，文字呈极其柔和的淡蓝色（`#64B5F6`）；若为 `suppressed`（被抑制），文字呈现微弱的警示橙色（`#FFB74D`）。
* **目标应用进程 (第三列)**：固定宽度 `100px`，文字高亮白，以便用户一眼看出是哪个软件（如 `SnippingTool`、`Code`）。
* **决策信度参数 (第四列)**：居右对齐，文字前景色为 `#8E8E93`。





---

## 4. 全局动画与窗口交互生命周期治理

* **窗口优雅唤醒 (macOS Motion Effects)**：
* 当用户打开设置页时，窗口不能突兀出现。`Storyboard` 必须在 `200ms` 内同步执行两个动效：
1. `Opacity` 从 `0` 线性平滑过渡到 `1`。
2. 配合 `ScaleTransform`，窗口在垂直和水平方向同时从 `0.95` 倍平滑膨胀放大至 `1.0` 倍，缓动函数采用弹性极佳的 `BackEase Out`。给用户一种设置面板轻快弹出的高档视觉。




* **数据流平滑加载态 (Testing Connection State)**：
* 当用户在底部点击 `[⚙️ 测试连接]` 时 ，该按钮内部的文本在 `100ms` 内渐隐，并居中替换为一个符合果系审美、直径 `14px` 的微型无限旋转光环（`Arc` 旋转动画），提示正在穿透网络。测试成功后，光环变为科技绿的 `✓`，文本更替为 `连接成功`，保持 1.5 秒后平滑恢复原貌。





这套**主窗口设置页全页 UI/UX 重构方案**在色彩、材质、控件细节上与你先前的翻译卡片进行了完美的深度对齐。你可以直接将整套规范贴进工程设计案中，Agent 将能够极其精准、像素级地还原出一套高内聚、工业级、充满极客美感的 Windows 全局 AI 转译生态界面！