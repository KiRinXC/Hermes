# Hermes UI 设计规范

本文档记录 Hermes 当前界面设计原则、视觉语言、组件规范和交互约束。`README.md` 面向用户，`Design.md` 面向实现和架构，`UI.md` 专注界面体验本身。

## 设计目标

Hermes 是 Windows 全局 AI 翻译助手，界面应当像一个轻量、可靠、低打扰的系统工具，而不是营销网站或重型管理后台。

核心感受：

- 快速：翻译入口短，加载和流式输出要让用户感到应用在即时响应。
- 克制：悬浮 UI 不抢当前应用的注意力，不遮挡过多内容。
- 清晰：按钮、状态、当前选项和错误反馈必须一眼可读。
- 精致：字重、间距、圆角和阴影保持细腻，避免粗重和堆砌。
- 安全：隐私、API Key、历史记录等信息用语要明确，让用户知道什么会被发送。

## 品牌语言

Hermes 的品牌符号来自红色章鱼脚图标。它表达“触手连接多个应用”的能力，但产品 UI 本体不应过度卡通化。

使用方式：

- 应用图标用于设置窗口 Header、托盘、翻译卡片品牌位和 README 视觉资产。
- 产品界面中章鱼符号保持小而明确，避免在工具界面里出现大面积插画。
- README 头图可以更有冲击力，但仍应服务于“跨应用翻译”这个产品信息。

README 头图当前使用：

```text
docs/assets/Info.png
```

文生图提示词归档在：

```text
docs/prompts/hermes-readme-hero.md
```

## 色彩

Hermes 支持浅色、深色和跟随系统主题。设置窗口拥有本地调色板，运行时替换 brush 资源，所有组件应通过 DynamicResource 跟随主题变化。

基础原则：

- 浅色模式：白色和浅灰作为主体，中性边框建立层次，蓝色只用于激活、焦点和主操作。
- 深色模式：接近黑色的背景，低透明白作为卡片和控件层次，避免纯黑大面积死板。
- 红色：主要是品牌图标和 README 视觉，不作为常规错误或危险状态的主色。
- 蓝色：用于主按钮、焦点、选中态和开关开启态。
- 黄色/绿色：仅用于警告和成功状态，不参与装饰。

当前设置窗口关键色：

```text
Dark shell:        #F20A0A0C
Light shell:       #F4F5F5F7
Accent:            #007AFF
Light keycap shell:#E5E5EA
Light keycap:      #F2F2F7
Light segment:     track #E5E5EA, indicator #FFFFFF, border #D1D1D6
```

## 字体和字重

字体使用 Windows 原生体系：

```text
Segoe UI Variable Display, Segoe UI
```

字重规则：

- 常规正文：Regular。
- 标题、页签、按钮、重要标签：Medium。
- 避免在常规 UI 中使用 Bold 或 SemiBold。
- 小字号文本不要通过加粗制造层级，应通过颜色、间距和位置建立层级。

字号倾向：

- 设置窗口正文：12-13 px。
- 设置分组标题：13 px Medium。
- 翻译卡片正文跟随用户设置，默认应舒适阅读。
- 头图、README 图片里的文字尽量少，避免模型生成不可读文字。

## 间距和圆角

整体界面遵循紧凑但不拥挤的桌面工具密度。

- 设置窗口固定 `800 x 600`。
- 设置窗口壳体圆角约 `14 px`。
- 设置卡片圆角约 `10-12 px`。
- 按钮和小控件圆角根据尺寸使用胶囊或 6-8 px。
- 图标与文字至少保持 `10 px` 间距。
- 设置 Header 品牌区与页签保持更舒展的垂直距离，避免粘连。

不要使用卡片套卡片。分组卡片内可以有行分隔和轻量说明，但不再额外包一层浮动卡片。

## 设置窗口

设置窗口是 Hermes 的主要配置界面，结构为 Header、Body、Footer。

Header：

- 左侧显示应用图标和 Hermes 名称。
- 右侧显示可点击录制的快捷键键帽。
- 页签为常规、翻译、外观、隐私、高级。
- 页签是文字导航，不使用图标，保持安静。

Body：

- 使用卡片承载每个设置分区。
- 布尔项使用 ToggleSwitch。
- 数值项使用 Slider 或紧凑输入框。
- 主题使用分段选择器。
- API Key 支持显示/隐藏，测试连接放在 API Key 上下文中。
- 高级页展示触发诊断和清空记录动作。

Footer：

- 保留状态文本和保存按钮。
- 快捷键录制过程不占用 Footer 状态提示。
- 非必要情况下不显示冗长指导语。

焦点行为：

- 鼠标点击页签切换时，不应自动把焦点显示在页面第一个开关上。
- 键盘导航仍保留可见焦点，保证可访问性。
- 快捷键栏进入录制时可以显示蓝色框；取消或完成后必须清除。

## 翻译浮动按钮

浮动按钮只在按住 Ctrl 完成文本选择后出现。普通拖选不触发，不记录诊断。

视觉：

- 外层为 `44 x 44` 透明命中区。
- 内部图标约 `25 x 25`。
- 未选中和未点击状态下图标保持清晰，不做模糊或低透明处理。
- 浅色和深色主题使用不同 SVG 资源。

资源位置：

```text
src/Hermes.Windows/Resources/Icons/FloatingButtonLight.svg
src/Hermes.Windows/Resources/Icons/FloatingButtonDark.svg
```

交互：

- 点击整个透明命中区都应触发翻译。
- 悬停和按下状态只做非常轻的背景反馈。
- 不抢当前应用焦点。
- 用户点击、滚动、切换窗口或超时后自动关闭。

## 翻译卡片

翻译卡片是主体验界面，应优先表现“快、清楚、可控”。

结构：

- 顶部使用应用图标作为品牌标识。
- 主体显示流式译文。
- 原文可以折叠或弱化显示。
- 操作区提供复制、重新翻译、固定、关闭。

行为：

- 翻译请求开始后立即显示加载态。
- 收到流式 delta 后逐段追加译文。
- 卡片完成且未固定时，点击外部区域关闭。
- 用户关闭卡片时取消当前请求。
- 非交互表面可拖动卡片；按钮、开关、滚动条等控件不触发拖拽。

视觉：

- 圆角柔和，阴影克制。
- 最大宽度约 `480 px`，最小宽度约 `280 px`。
- 文本对比度必须优先于装饰。
- 错误状态用明确短句，不展示技术栈噪音。

## 托盘界面

托盘是低频控制入口，不应承载复杂信息。

当前交互：

- 左键单击托盘图标打开设置。
- 右键菜单提供暂停/恢复、翻译剪贴板、设置、退出等动作。
- 托盘菜单不展示历史入口。

视觉：

- 小窗口轻量、紧凑、清晰。
- 图标和文字间距保持一致。
- 不使用重字重制造强调。

## README 视觉

README 是外部用户对 Hermes 的第一印象。它应传达三个信息：

1. Hermes 是一个跨应用翻译工具。
2. 它是 Windows 桌面应用，不是浏览器插件。
3. 它有明确的品牌记忆点。

头图原则：

- 可以有章鱼角色和冲击感，但产品信息要清楚。
- 不展示 Windows 任务栏、桌面壁纸或伪系统截图。
- App 图标尽量使用真实、清晰、少量的卡片，不堆满画面。
- Zotero 等容易生成错误的图标，优先后期手动贴真实资源。
- 图片在 GitHub README 宽度下仍要看得清中心主体。

## 可访问性

- 关键控件需要 AutomationProperties.Name。
- 键盘导航保留可见焦点。
- 焦点视觉只在用户真正需要时出现，避免鼠标操作时产生误导。
- 图标按钮需要 Tooltip 或可访问名称。
- 浅色和深色主题都要保证文本对比度。

## 变更约束

后续 UI 变更必须同步检查：

- README 是否仍描述当前产品状态。
- UI.md 是否仍描述当前视觉和交互规范。
- Design.md 是否记录了功能、结构、打包或策略变化。
- 测试是否覆盖容易回退的 UI 约束，例如字重、主题资源、焦点行为和图标命中区。
