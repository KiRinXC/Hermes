下面是一份可以直接交给开发 agent 的需求细化稿。我会把它定义为 **Windows 系统级 AI 翻译助手**，而不是传统意义上的“浏览器插件”。因为你的目标是“任何应用中选中文本即可翻译”，这在 Windows 上本质是一个常驻桌面应用：监听选区、显示悬浮按钮、调用 AI API、展示结果。

---

# Windows 全局 AI 划词翻译助手需求说明

## 1. 产品定位

做一个仅面向 Windows 的轻量级全局翻译工具。用户在任意 Windows 应用中选中一段英文文本后，界面附近自动出现一个小型翻译按钮。用户点击按钮后，应用调用 OpenAI / ChatGPT API，将选中文本翻译为中文，并以美观、轻量、不中断当前工作流的悬浮卡片展示翻译结果。

产品关键词：

**简洁、美观、快速、低打扰、全局可用、英文到中文优先、AI 翻译质量高。**

---

## 2. 核心用户场景

### 场景 A：划词后点击按钮翻译

用户在 Chrome、Edge、PDF 阅读器、VS Code、Notion、Slack、Word、邮件客户端等应用中选中一段英文文本。

选区右上角或鼠标附近出现一个小型圆角按钮，按钮上显示：

> 译

用户点击后，出现翻译卡片。

卡片展示：

* 原文摘要或折叠原文
* 中文翻译
* 复制译文按钮
* 重新翻译按钮
* 关闭按钮
* 可选：固定窗口按钮

### 场景 B：快捷键翻译

如果某些应用无法准确识别选中文本，用户可以选中文本后按快捷键：

> Ctrl + Alt + E

应用自动尝试读取当前选中文本并翻译。

### 场景 C：复制后翻译

用户复制一段英文文本后，托盘菜单或快捷键可以翻译剪贴板内容。

这是兼容性兜底方案。

---

## 3. 重要技术现实边界

“任何应用选中文本后，右上角弹按钮”在 Windows 上不能做到 100% 完美，因为不同应用暴露文本选区的方式不同。推荐实现为：

1. **优先使用 Windows UI Automation 读取选中文本。**
   Microsoft UI Automation 的 `TextPattern` 用于暴露文本控件内容，包括文本范围、选区和相关属性，适合读取支持 UI Automation 的文本控件。([Microsoft Learn][1])

2. **用全局鼠标/键盘监听判断用户是否完成选区操作。**
   Windows 的 `SetWindowsHookEx` 可以安装 hook 来监听同一桌面内的系统事件，适合做全局鼠标释放、快捷键触发等输入监控。([Microsoft Learn][2])

3. **如果 UI Automation 无法获取选区，则使用受控剪贴板兜底。**
   即在用户点击按钮或按快捷键时，程序短暂模拟 `Ctrl+C`，读取剪贴板文本，然后恢复原剪贴板内容。这个方式兼容性高，但必须谨慎处理隐私、剪贴板污染和密码输入框。

所以需求表述应从“任何应用 100% 右上角弹按钮”调整为：

> 在主流 Windows 桌面应用中，选中文本后尽可能自动显示翻译按钮；无法获取精确选区位置时，按钮显示在鼠标附近；无法读取选区时，提供快捷键和剪贴板翻译兜底。

---

## 4. 推荐技术栈

### 首选方案

**C# / .NET 10 + WPF + Win32 API + UI Automation + OpenAI Responses API**

理由：

* Windows 原生能力强，适合做全局鼠标监听、快捷键、托盘、悬浮窗、剪贴板、UI Automation。
* WPF 做透明悬浮窗、圆角卡片、DPI 适配、无焦点弹窗更直接。
* .NET 10 是 LTS 版本，适合做长期维护的 Windows 桌面应用。([Microsoft Learn][3])
* OpenAI 官方现在推荐文本生成类应用优先使用 Responses API，而不是旧的 Chat Completions API。([OpenAI平台][4])

### 不推荐首选 Electron / Tauri

Electron 和 Tauri 做 UI 很方便，但这个产品的核心不是普通桌面窗口，而是 Windows 全局选区识别、系统级悬浮层、低级输入监听和剪贴板控制。用 C# / WPF / Win32 会更直接、更稳定、更少桥接成本。

### 可选增强

如果开发者更擅长 Web 前端，可以使用：

**WPF 壳 + WebView2 渲染设置页或翻译卡片**

WebView2 可以在 WPF 应用中嵌入 HTML / CSS / JavaScript UI。([Microsoft Learn][5])

但 MVP 阶段建议直接用 WPF/XAML 实现 UI，减少复杂度。

---

# 5. 功能需求

## 5.1 常驻后台

应用启动后驻留系统托盘。

托盘菜单包括：

* 开启 / 暂停划词翻译
* 翻译剪贴板
* 设置
* 查看历史
* 退出

默认开机自启动可选，不强制。

---

## 5.2 选区检测

应用需要监听以下行为：

* 鼠标左键按下
* 鼠标拖动
* 鼠标左键释放
* 双击选词
* 快捷键触发

当检测到用户可能完成选区后：

1. 获取当前前台窗口。
2. 通过 UI Automation 尝试读取当前选中文本。
3. 判断文本是否有效。
4. 如果文本长度在合理范围内，显示悬浮翻译按钮。

有效文本判断：

* 去除首尾空格后长度大于 1。
* 默认只对包含英文字符的文本显示按钮。
* 文本长度上限 MVP 可设为 5000 字符。
* 如果文本过长，按钮仍可显示，但翻译前提示“文本较长，可能需要更久”。

---

## 5.3 悬浮翻译按钮

按钮样式：

* 小型圆角胶囊按钮
* 文案：`译`
* 尺寸：约 36 × 32 px
* 半透明背景
* 阴影
* 鼠标悬停时放大或高亮
* 不抢焦点
* 3 秒内无操作自动淡出
* 用户继续点击、滚动、切换窗口时自动关闭

定位规则：

1. 如果可以获取选区矩形，则显示在选区右上角。
2. 如果无法获取选区矩形，则显示在鼠标释放位置附近。
3. 不允许超出屏幕边界。
4. 多显示器环境下，应显示在当前鼠标所在屏幕。

---

## 5.4 翻译卡片

用户点击“译”按钮后，展示翻译结果卡片。

卡片 UI：

* 圆角：16px 以上
* 阴影柔和
* 最大宽度：480px
* 最小宽度：280px
* 深色 / 浅色主题自动适配系统
* 支持拖动
* 支持关闭
* 支持复制译文
* 支持重新翻译
* 支持固定卡片
* 支持加载态骨架屏或 spinner

卡片内容：

```text
翻译
[中文译文内容]

操作：
复制 | 重新翻译 | 固定 | 关闭
```

可选高级模式：

* 展开原文
* 显示“直译 / 自然 / 专业”切换
* 显示术语解释
* 显示简短总结

MVP 只做默认自然中文翻译即可。

---

## 5.5 快捷键

默认快捷键：

> Ctrl + Alt + E

行为：

1. 尝试读取当前选中文本。
2. 如果失败，尝试受控复制读取。
3. 调用翻译。
4. 在鼠标附近展示结果卡片。

设置页允许修改快捷键。

---

## 5.6 剪贴板翻译兜底

托盘菜单增加：

> 翻译剪贴板

读取当前剪贴板文本并翻译。

快捷键翻译选区时，如果需要模拟 `Ctrl+C`：

* 先保存当前剪贴板内容。
* 模拟复制。
* 等待 100–300ms。
* 读取文本。
* 恢复原剪贴板内容。
* 不在密码框、受保护窗口中执行自动复制。

---

## 5.7 AI 翻译能力

使用 OpenAI Responses API。

OpenAI Responses API 支持提交文本输入并生成文本或 JSON 输出。([OpenAI平台][6])

### 模型配置

不要把模型名硬编码死。设置页提供模型输入框。

默认建议：

```text
gpt-4.1-mini 或其他低延迟、低成本文本模型
```

如果用户有更高质量需求，可手动改为更强模型。

### 翻译 Prompt

建议系统指令：

```text
你是专业英文到简体中文翻译引擎。请将用户提供的英文内容翻译成自然、准确、流畅的简体中文。

要求：
1. 保留原文含义，不添加原文没有的信息。
2. 保留代码、命令、URL、变量名、品牌名、专有名词。
3. 技术术语要准确，必要时使用中文术语并保留英文原词。
4. 保留原文段落结构。
5. 不要解释，不要总结，不要输出多余前后缀，只输出译文。
```

用户输入：

```text
Translate the following text into Simplified Chinese:

<selected_text>
```

### API 返回处理

需要处理：

* 网络超时
* API key 无效
* 余额不足
* 速率限制
* 文本过长
* 返回为空
* 用户取消请求

加载超过 8 秒时，显示：

> 翻译时间较长，请稍候…

---

## 5.8 设置页

设置项包括：

### API 设置

* API Provider：OpenAI / OpenAI-compatible
* Base URL
* API Key
* Model
* 测试连接按钮

API Key 必须本地加密存储，不能明文写入配置文件。

### 翻译设置

* 源语言：默认自动识别，MVP 可固定英文
* 目标语言：简体中文
* 翻译风格：

  * 自然
  * 直译
  * 专业
  * 简洁
* 最大翻译字符数
* 是否保留原文格式

### 触发设置

* 选中文本后自动显示按钮：开 / 关
* 快捷键
* 复制后是否提示翻译：开 / 关
* 排除应用列表

### UI 设置

* 浅色 / 深色 / 跟随系统
* 卡片宽度
* 字体大小
* 透明度
* 是否固定翻译卡片

### 隐私设置

* 是否保存翻译历史
* 是否保存原文
* 一键清空历史
* 排除敏感应用

---

# 6. 非功能需求

## 6.1 性能

* 后台常驻内存尽量低。
* 空闲时 CPU 接近 0。
* 鼠标 hook 不允许做重计算，只负责事件转发。
* UI Automation 查询要节流，避免每次鼠标移动都查询。
* 翻译请求异步执行，不阻塞 UI。

## 6.2 兼容性

目标系统：

* Windows 10
* Windows 11

重点测试应用：

* Chrome
* Edge
* Firefox
* Notepad
* Word
* Outlook
* VS Code
* Slack / Discord / Teams
* PDF 阅读器
* Obsidian / Notion 桌面版

## 6.3 安全与隐私

必须做到：

* API Key 本地加密存储。
* 默认不保存翻译历史，或者首次启动时询问用户。
* 不自动翻译密码框内容。
* 不上传用户未主动触发的文本。
* 日志中不能记录 API Key。
* 日志中默认不记录完整原文和译文。

## 6.4 稳定性

* 应用异常不能影响其他应用输入。
* hook 崩溃后可以自动恢复。
* API 请求失败时 UI 友好提示。
* 多显示器、高 DPI、缩放比例 125% / 150% 下正常显示。

---

# 7. 推荐架构

```text
AITranslator.Windows
├─ App.xaml / App.xaml.cs
├─ MainWindow              # 设置页或主窗口
├─ Tray
│  └─ TrayService
├─ Input
│  ├─ MouseHookService
│  ├─ KeyboardHookService
│  └─ HotkeyService
├─ Selection
│  ├─ ISelectionProvider
│  ├─ UiAutomationSelectionProvider
│  ├─ ClipboardSelectionProvider
│  └─ SelectionResult
├─ Overlay
│  ├─ FloatingButtonWindow
│  ├─ TranslationPopupWindow
│  └─ OverlayPositionService
├─ Translation
│  ├─ ITranslationService
│  ├─ OpenAITranslationService
│  ├─ TranslationRequest
│  └─ TranslationResult
├─ Settings
│  ├─ SettingsService
│  ├─ SecretStorageService
│  └─ AppSettings
├─ History
│  └─ TranslationHistoryService
├─ Infrastructure
│  ├─ Logger
│  ├─ HttpClientFactory
│  └─ ErrorMapper
└─ UI
   ├─ Controls
   ├─ Themes
   └─ Icons
```

---

# 8. 核心实现路线

## 阶段 1：项目骨架

目标：

* 创建 WPF 桌面应用。
* 支持系统托盘。
* 支持设置窗口。
* 支持本地配置保存。
* 支持 API Key 加密保存。
* 支持基本日志。

交付标准：

* 应用启动后出现在系统托盘。
* 可以打开设置页。
* 可以填写 API Key、Base URL、Model。
* 可以点击“测试连接”。

---

## 阶段 2：快捷键翻译 MVP

先不要做复杂划词悬浮按钮，先实现最稳定闭环。

目标：

* 用户选中文本。
* 按 `Ctrl + Alt + E`。
* 程序读取选区或复制选区。
* 调用 OpenAI API。
* 显示翻译卡片。

交付标准：

* 在 Chrome、Edge、Notepad、VS Code 中可用。
* 翻译卡片可关闭、复制译文。
* API 错误有明确提示。

---

## 阶段 3：划词后显示小按钮

目标：

* 鼠标拖选文本后自动显示“译”按钮。
* 点击按钮后翻译。
* 如果无法获取选区位置，则按钮显示在鼠标附近。

交付标准：

* 主流应用中选中文本后按钮出现。
* 按钮不抢焦点。
* 点击空白处或切换窗口后按钮消失。
* 多显示器位置正确。

---

## 阶段 4：UI 打磨

目标：

* 优化按钮动画。
* 优化翻译卡片。
* 增加加载态。
* 增加暗色模式。
* 增加固定卡片。
* 增加拖动卡片。
* 增加原文折叠。

交付标准：

* UI 观感接近 Raycast / PopClip / Arc 风格。
* 操作路径短。
* 用户无需打开主窗口即可完成翻译。

---

## 阶段 5：兼容性和兜底

目标：

* 增加应用黑名单。
* 增加剪贴板翻译。
* 增加失败重试。
* 增加超长文本提示。
* 增加翻译历史开关。

交付标准：

* 不支持 UI Automation 的应用仍可用快捷键翻译。
* 复制兜底不会永久污染剪贴板。
* 用户可以关闭自动划词按钮。

---

## 阶段 6：打包发布

目标：

* 打包为 Windows 安装包。
* 支持开机自启动选项。
* 支持自动更新，可后续实现。
* 支持便携版，可选。

交付标准：

* 一键安装。
* 卸载干净。
* 不需要用户手动配置复杂环境。

---

# 9. MVP 范围

第一版只做这些：

1. Windows 10 / 11 桌面应用。
2. 系统托盘常驻。
3. 设置 API Key、Base URL、Model。
4. 快捷键翻译当前选中文本。
5. 鼠标选中文本后显示小型“译”按钮。
6. 点击按钮后展示中文翻译。
7. 复制译文。
8. 关闭翻译卡片。
9. 暗色 / 浅色基本适配。
10. 剪贴板兜底读取。

第一版不做：

* OCR 截图翻译
* 多语言互译
* 术语库
* 云同步
* 账号系统
* 浏览器插件
* 手机端
* 团队协作
* PDF 图片文字识别
* 离线模型

---

# 10. 验收标准

## 基础功能验收

* 在 Chrome 中选中英文段落，点击悬浮按钮，可以得到中文翻译。
* 在 Notepad 中选中英文文本，按快捷键，可以得到中文翻译。
* 在 VS Code 中选中注释或 README 文本，可以翻译。
* 翻译结果可以一键复制。
* 无网络时显示明确错误。
* API Key 错误时显示明确错误。
* 不选中文本时按快捷键，不应弹出空白翻译卡片。

## UI 验收

* 悬浮按钮不能遮挡太多内容。
* 翻译卡片不能抢走当前应用焦点。
* 卡片位置不能超出屏幕。
* 多显示器下显示在当前屏幕。
* 关闭动画自然。
* 暗色模式下可读性良好。

## 稳定性验收

* 连续使用 30 分钟不崩溃。
* 快速多次划词不会出现多个残留按钮。
* 切换应用后旧按钮消失。
* API 请求失败不会导致主程序退出。

---

# 11. 推荐给开发 agent 的实现优先级

按这个顺序做：

1. **先做快捷键翻译闭环。**
2. **再做 OpenAI 翻译服务封装。**
3. **再做翻译卡片 UI。**
4. **再做鼠标划词检测。**
5. **最后做选区右上角精准定位。**

不要一开始就追求“任何应用右上角精准弹出”。那是最难、最不稳定的部分。先确保用户能通过快捷键稳定翻译，再逐步增强自动划词体验。

---

# 12. 给 agent 的一句话任务描述

> 请实现一个 Windows 10/11 桌面常驻翻译助手，使用 C# / .NET 10 / WPF 开发。应用监听用户选中文本和快捷键操作，在主流 Windows 应用中显示一个轻量悬浮“译”按钮，点击后调用 OpenAI Responses API 将英文翻译为简体中文，并以美观的悬浮卡片展示结果。需要支持快捷键翻译、剪贴板兜底、API Key 本地加密存储、托盘菜单、设置页、暗色模式和基础错误处理。MVP 优先保证快捷键翻译和主流应用可用性，再逐步优化自动划词按钮和选区定位。

